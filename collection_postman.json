{
  "info": { "name": "♟️ Chess API - CLEAN" },
  "variable": [
    { "key": "base_url", "value": "https://chess-api.up.railway.app" },
    { "key": "jwt_token", "value": "" }
  ],
  "item": [
    {
      "name": "🔐 1. REGISTER (FIRST!)",
      "request": {
        "method": "POST",
        "url": "{{base_url}}/api/auth/register",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"username\": \"user{{$timestamp}}\",\n  \"email\": \"user{{$timestamp}}@test.com\",\n  \"password\": \"password123\",\n  \"firstName\": \"Test\",\n  \"lastName\": \"User\",\n  \"role\": \"ADMIN\"\n}"
        }
      }
    },
    {
      "name": "👥 2. Get All Users (PUBLIC)",
      "request": {
        "method": "GET",
        "url": "{{base_url}}/api/users"
      }
    },
    {
      "name": "♟️ 3. Create Game (WITH JWT)",
      "request": {
        "method": "POST",
        "url": "{{base_url}}/api/games",
        "header": [{"key": "Authorization", "value": "Bearer {{jwt_token}}"}],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"whitePlayerId\": 1,\n  \"blackPlayerId\": 1,\n  \"timeControl\": \"BLITZ\"\n}"
        }
      }
    },
    {
      "name": "📋 4. Get All Games (PUBLIC)",
      "request": {
        "method": "GET",
        "url": "{{base_url}}/api/games"
      }
    }
  ]
}